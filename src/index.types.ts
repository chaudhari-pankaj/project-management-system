export type project = {
    id : number,
    name : string,
    organisation_id : number,
}

export type member = {
    id : number,
    name : string,
    email : string,
    organisation_id : number,
}