export type project = {
    id : number,
    name : string,
    organization_id : number,
}

export type member = {
    id : number,
    name : string,
    email : string,
    organization_id : number,
}